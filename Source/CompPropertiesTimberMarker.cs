using Verse;

namespace TimberMarker
{
    // CompProperties（放在同一命名空间下）
    public class CompProperties_TimberMarker : CompProperties
    {
        public CompProperties_TimberMarker()
        {
            this.compClass = typeof(CompTimberMarker);
        }
    }

}